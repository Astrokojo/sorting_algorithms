#include "sort.h"
/**
 * sort_sort - Sorts doubly linked lists of int in ascending order
 * @list: the list
 *
 */
void insertion_sort_list(listint_t **list)
{
    listint_t *temp;
    int index;

    if(list == NULL && index > 0)
    

    /*for (*list) 
        if list->prev > list->node
            temp_node = list->prev
            list->prev = list_node
            list_node = temp_node

    */
}